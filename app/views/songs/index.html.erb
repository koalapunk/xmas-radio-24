
<div> <img src="images/Titel.png" style="width:300px;height:400px;"></div>
<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>

  <% content_for :title, "Songs" %>

  <div class="flex justify-between items-center">
    <p> Viel Spaß beim Runterladen und Zuhören! Zum Download einfach auf die .wav-Datei klicken :) </p>
    <%= link_to "NEUEN SONG HINZUFÜGEN", new_song_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-large italic clear-right" %>
    <br>
    </br>
    <br>
    </br>
    </div>

  <div id="songs" class="min-w-full">
    <% @songs.each do |song| %>
      <%= render song %>
      <p>
        <%= link_to "song bearbeiten", song, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium italic" %>
      </p>
      <br></br>
    <% end %>
  </div>
</div>
